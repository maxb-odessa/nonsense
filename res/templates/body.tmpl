<thead>
    <tr>
        <td class="sysinfo" colspan="{{ len .Columns }}">
            <div style="display: inline-grid; grid-template-columns: 33% 33% 34%; grid-template-rows: 50% 50%; width: 100%;">
                <div class="sysinfo-time" id="sysinfo-time"></div>
                <div class="sysinfo-mem" id="sysinfo-mem">mem here</div>
                <div class="sysinfo-mem" id="sysinfo-mem">something here</div>
                <div class="sysinfo-la" id="sysinfo-la"></div>
                <div class="sysinfo-mem" id="sysinfo-mem">dunno #1</div>
                <div class="sysinfo-buttons"><button onClick="return saveConfig();">Save</button></div>
            </div>
        </td>
    </tr>
</thead>

<tbody>
    <tr id="columns" data-columns-num="{{ len .Columns }}">
    {{ range $ci, $column := .Columns }}

        <td
            class="column"
            id="{{ $ci }}"
            data-groups-num="{{ len $column.Groups }}">
            {{ range $gi, $group := $column.Groups }}

                <fieldset
                    class="group"
                    id="{{ $group.Id }}"
                    name="group"
                    data-group-name="{{ $group.Name }}"
                    data-sensors-num="{{ len $group.Sensors }}"
                    data-column="{{ $ci }}"
                    title="click to edit or move this group">

                    <legend onClick="editGroup('{{ $group.Id }}');" class="group-name">
                        &nbsp;{{ $group.Name }}&nbsp;
                    </legend>

                    {{ range $si, $sensor := $group.Sensors }}
                        <div id="json-{{ $sensor.Id }}" hidden>{{ $sensor.Json }}</div>
                        <div
                            class="sensor"
                            id="{{ $sensor.Id }}"
                            name="sensor"
                            data-sensor-name="{{ $sensor.Name }}"
                            data-group-id="{{ $group.Id }}"
                            title="click to edit this sensor"
                            onClick="editSensor('{{ $sensor.Id }}');">
                        </div>
                    {{ end }}

                </fieldset>

            {{ end }}

        </td> <!-- groups -->
    {{ end }}

    </tr> <!-- columns -->
</tbody>
